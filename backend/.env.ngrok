: ${EMAIL_USER:=enterochad@gmail.com}
: ${EMAIL_PASSWORD:=jwpymmwjhldvmmbn}
: ${EMAIL_SERVICE:=gmail}

# NGrok Configuration
NGROK_ENABLED=false
# Set to your ngrok URL when tunneling: https://xxxx-xxx-xxx.ngrok-free.app
NGROK_URL=

# Fallback to display PIN on frontend if email fails
PIN_DISPLAY_ON_FAILURE=true

# Request timeout for email sending (important for ngrok)
EMAIL_TIMEOUT=5000
